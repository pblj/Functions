#pragma once
#include "constants.h"
#include "stdafx.h"

template <typename T>
void printArray(T arr[], const int n);
template <typename T>
void printArray(T arr[ROWS][COLS], const int ROWS, const int COLS);